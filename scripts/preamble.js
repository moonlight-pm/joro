// const fs = require('fs')
// const path = require('path')
// const dotenv = require('dotenv')
// const { red, yellow, magenta } = require('colorette')

module.exports = async (options) => {
  // const project = path.resolve(__dirname, '..')
  // const env = options.env || process.env.NODE_ENV || 'development'
  // options.env = env
  // console.log(`             ${'-'.repeat(env.length + 8)}`)
  // console.log(`${yellow(`Environment:`)} >>> ${magenta(env)} <<<`)
  // console.log(`             ${'-'.repeat(env.length + 8)}`)
  // const envfile = path.resolve(project, `.env${env === 'development' ? '' : '.' + env}`)
  // if (fs.existsSync(envfile)) {
  //   dotenv.config({ path: envfile })
  // } else {
  //   console.log(`${red(`No dotenv file '${envfile}' found`)}`)
  // }
}
